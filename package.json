{
  "name": "dental-clinic-agent",
  "version": "1.0.0",
  "description": "WhatsApp automation agent for a small dental clinic: booking, reminders, rescheduling, FAQ, follow-ups, recall, no-show handling and review requests — with strict medical-safety guardrails.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "seed": "node scripts/seed.js",
    "simulate": "node scripts/simulate.js",
    "scheduler:once": "node scripts/run-scheduler-once.js",
    "test": "node test/run-tests.js"
  },
  "engines": { "node": ">=22.13" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
